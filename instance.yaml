heat_template_version: 2015-04-30

description: Simple template to deploy a single compute instance

resources:
  my_instance:
    type: OS::Nova::Server
    properties:
      name: my_instance
      image: Fedora
      flavor: m1.small
      key_name: keypair
      networks:
        - network: Private
      
